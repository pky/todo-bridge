rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // バックアップファイルはCloud Functionsからのみアクセス可能
    // クライアントからの直接アクセスは禁止
    match /backups/{userId}/{allPaths=**} {
      allow read, write: if false;
    }
  }
}
